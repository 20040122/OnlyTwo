"use server";

export async function login() {
  return { message: "TODO: sign in with Supabase Auth." };
}

export async function signup() {
  return { message: "TODO: create a Supabase Auth user." };
}

export async function signOut() {
  return { message: "TODO: sign out the current session." };
}
